export interface MatchDto {
    host: string;
    guest: string;
    hostSetsResult: number;
    guestSetsResult: number;
    round: number;
    status: string;
    matchDate: string;
    court: string;
}